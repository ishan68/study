Pushing directly to server with git hooks
create deployment folder on server
eg - mkdir /var/www/html
create folder for repo on server
e.g - mkdir /home/ubuntu/newproj.git
cd  /home/ubuntu/newproj.git
git init --bare
cd hooks
create file - vim post receive
#!/bin/bash
TARGET="/var/www/html/"
GIT_DIR="/home/ubuntu/newproj.git"
BRANCH="master"

while read oldrev newrev ref
do
        # only checking out the master (or whatever branch you would like to deploy)
        if [ "$ref" = "refs/heads/$BRANCH" ];
        then
                echo "Ref $ref received. Deploying ${BRANCH} branch to production..."
                git --work-tree=$TARGET --git-dir=$GIT_DIR checkout -f $BRANCH
        else
                echo "Ref $ref received. Doing nothing: only the ${BRANCH} branch may be deployed on this server."
        fi
done
~    


chmod -R 777 /home/ubuntu/git_repos/
create a repo on github
clone it onto local
go into cloned folder
add remote
eg for aws
ssh-add path/to/your/EC2.pem
git remote add production  ubuntu@3.133.59.199:/home/ubuntu/git_repos/newproj.git
git config --global remote.production.receivepack "git receive-pack"
vim index.html
git add .
git commit -m "test"
git push production master
